/**
 * $Id: BuildUtils.java 115453 2012-11-13 07:10:48Z wei.cheng@dajie-inc.com $
 * Copyright 2009-2013 DAJIE-INC. All rights reserved.
 */
package com.dajie.mobile.mcp.api.utils;

/*
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import net.sf.json.JSONObject;*/

//import org.apache.commons.logging.Log;
//import org.apache.commons.logging.LogFactory;

/**
 * 
 * 
 * @author <a href="mailto:wei.cheng3@renren-inc.com">程伟</a>
 * @version 2012-9-26下午01:32:55
 */
public class BuildUtils {

    //private static final Logger logger = LoggerFactory.getLogger(BuildUtils.class);

    /*
     * public static JSONObject buildResult(Object result) {
     * 
     * JSONObject jo = null;
     * 
     * if (result instanceof Integer || result instanceof Long || result
     * instanceof Boolean) {
     * 
     * Map<String, Object> rtMap = new HashMap<String, Object>();
     * rtMap.put("result", result); jo = JSONObject.fromObject(rtMap);
     * 
     * } else { jo = JSONObject.fromObject(result);
     * 
     * } logger.debug(jo); return jo; }
     */
    //    public static JSONObject buildResult(Object result) {
    //
    //        JSONObject jo = null;
    //
    //        if (result instanceof List || result instanceof Integer || result instanceof Long
    //                || result instanceof Boolean || result instanceof String) {
    //            Map<String, Object> rtMap = new HashMap<String, Object>();
    //            rtMap.put("result", result);
    //            jo = JSONObject.fromObject(rtMap);
    //        } else {
    //            jo = JSONObject.fromObject(result);
    //        }
    //        logger.debug(jo);
    //        return jo;
    //    }
}
